{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "imports": {
    "#prisma": "./generated/prisma/client.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --ignore swagger-output.json --exec \"node src/swagger.js && node src/server.js\"",
    "pgen": "npx prisma generate",
    "mig": " npx prisma migrate dev --name",
    "dbpull": "npx prisma db pull",
    "dbpush": "npx prisma db push",
    "build": "esbuild src/server.js --bundle --platform=node --format=esm --packages=external --outfile=server.js",
    "build2": "esbuild src/server.js --bundle --platform=node --format=esm --external:#prisma --packages=external --outfile=server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/adapter-mariadb": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "axios": "^1.18.0",
    "bcryptjs": "^3.0.3",
    "chalk": "^5.6.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "nodemon": "^3.1.14",
    "swagger-autogen": "^2.23.7",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
